feature: "admin-background-delete"
status: "locked"
locked_hash: "5848fe57818556127df4640152b881b92346145cb7011df2280cbdb52fd17555"

# Hedef repo: https://github.com/serhandenizhan/kuyumcukent-project
# Kapsam: backend/app/routers/backgrounds.py içine DELETE /api/admin/backgrounds/{id}
# endpoint'i eklemek. POST /api/admin/backgrounds (yükleme) zaten var; silme yok.

criteria:
  - id: AC-01
    description: "Doğru X-Admin-Secret ile DELETE /api/admin/backgrounds/{id} çağrılırsa arka plan silinir (is_active=false veya kayıt kaldırılır) ve R2'deki dosya nesnesi de silinir"
  - id: AC-02
    description: "X-Admin-Secret header eksik veya yanlışsa istek 401 ile reddedilir, hiçbir şey silinmez"
  - id: AC-03
    description: "ADMIN_UPLOAD_SECRET ortam değişkeni tanımlı değilse istek 503 ile reddedilir (sessizce açık bırakılmaz)"
  - id: AC-04
    description: "Var olmayan bir id ile silme denenirse 404 döner"
  - id: AC-05
    description: "Silinen arka plan, silme sonrası GET /api/backgrounds listesinde artık görünmez"
  - id: AC-06
    description: "R2'den nesne silme başarısız olursa DB kaydı ile depolama arasında tutarsızlık oluşmaz (DB kaydı silinmeden önce R2 silme başarılı olmalı, ya da hata durumunda kayıt is_active=false kalıp iş idempotent şekilde tekrar denenebilir olmalı)"
